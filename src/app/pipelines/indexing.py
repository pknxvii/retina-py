def run_indexing_pipeline(doc_id: str, content: str):
    # TODO: setup Haystack pipeline
    # Example:
    # pipeline = Pipeline.load_from_yaml("pipeline_config.yaml")
    # pipeline.run({"documents": [content]})
    print(f"Running indexing pipeline for doc {doc_id}")
